version: 0.2
phases:
  pre_build:
    commands:
      - echo "Installing Maven..."
      - mvn --version
  build:
    commands:
      - echo "Building the Java Maven project..."
      - mvn clean install
artifacts:
  files: '**/*'
